# salestars

After cloning, run:

```bash
yarn install
```

Build it:
```bash
yarn build
```

Run it with a fake backend by running, in parallell:

```bash
yarn dyson
yarn start
```

Point your browser to http://localhost:8080
